import React from 'react';
import { Header, HeaderTitle } from './header.styled';

export const HeaderComponent = () => {
  return (
    <Header>
      <HeaderTitle>Giphy API</HeaderTitle>
    </Header>
  );
};
